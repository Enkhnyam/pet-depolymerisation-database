# How every number is arrived at

Generated by `tools/derivation.py` on 2026-09-04 from artifact set `74a63eabd1dc`, commit `2538ac4`. Do not edit: it is rewritten by `scripts/paper.sh`, and `--check` fails if it has gone stale.

Nothing in this file is transcribed. The attribution is parsed out of `tools/paper_numbers.py` and the figure modules, and the code blocks are the source lines that do the work, so this document cannot describe a calculation the code does not perform.

## What everything is computed from

Seven inputs. The artifact hash above is taken over all of them, so changing any one changes the hash the manuscript declares.

| input | path |
| --- | --- |
| database | `_setup.DATABASE` |
| database judge | `_setup.DATABASE_JUDGE` |
| benchmark | `_setup.EXTRACTION` |
| benchmark judge | `_setup.JUDGE` |
| adjudication | `adjudicated.REAL` |
| curated table | `artifacts/data/curated_table_final.json` |
| corpus markdown | `artifacts/data/corpus_markdown` |

## The figures, panel by panel

### `fig_database` &rarr; `artifacts/figures/fig_database.pdf`

| panel | computed by | which reads |
| --- | --- | --- |
| **a** How many experiments a paper reports | `checks/**/corpus.py` | corpus = 'corpus_markdown'; extraction = DATABASE; funnel = 'corpus_candidates.csv' |
| **b** Which route, as a part of the whole | `checks/**/chemistry.py` | corpus = 'corpus_markdown'; extraction = DATABASE |
| **c** How often each field is reported at all | `checks/**/chemistry.py` | corpus = 'corpus_markdown'; extraction = DATABASE |
| **d** The commonest catalysts, by substance | `checks/**/chemistry.py` | corpus = 'corpus_markdown'; extraction = DATABASE |
| **e** What the judge rewrote | `checks/**/verdicts.py` | extraction = DATABASE; judge = DATABASE_JUDGE |
| **f** What stops a record getting a route | `checks/**/chemistry.py` | corpus = 'corpus_markdown'; extraction = DATABASE |

#### Panel a — How many experiments a paper reports

The check behind it asks: *The funnel from search to corpus, and what the extraction produced on it.*

`figures/fig_database.py`, lines 38&ndash;39, 41&ndash;41:

```python
    step_hist(panel[0], per_paper, bins=int(per_paper.max()),
              xlabel="experiments reported per paper", ylabel="papers")

    headroom(panel[0], float(counts.max()))
```

#### Panel b — Which route, as a part of the whole

The check behind it asks: *Does the extracted database behave like chemistry?*

`figures/fig_database.py`, lines 54&ndash;55, 56&ndash;56:

```python
    pie(panel[1], ordered, colours=[ROUTE[name] for name in routes.index if name in short],
        span=1.7)

    panel[1].set_xlabel(f"route, of {int(ordered.sum()):,} records")
```

#### Panel c — How often each field is reported at all

The check behind it asks: *Does the extracted database behave like chemistry?*

`figures/fig_database.py`, lines 58&ndash;60:

```python
    # --- c: how often each field is reported at all ------------------------------------------
    ranked_bars(panel[2], chemistry["completeness"] * 100,
                xlabel="records reporting the field (%)", fmt="{:.0f}%")
```

#### Panel d — The commonest catalysts, by substance

The check behind it asks: *Does the extracted database behave like chemistry?*

`figures/fig_database.py`, lines 62&ndash;66:

```python
    # --- d: the commonest catalysts, by substance --------------------------------------------
    # "none" is the uncatalysed baselines and the largest single group, which is the finding, so
    # it takes the dark end of the ramp. Emphasis by weight, not by a second hue.
    ranked_bars(panel[3], chemistry["substances"].head(10), accent=EMPHASIS,
                xlabel="records (catalysts grouped by structure)")
```

#### Panel e — What the judge rewrote

The check behind it asks: *The judge's verdicts on the database.*

`figures/fig_database.py`, lines 71&ndash;72:

```python
    ranked_bars(panel[4], fields, accent=EMPHASIS,
                xlabel="records whose field the judge corrected")
```

#### Panel f — What stops a record getting a route

The check behind it asks: *Does the extracted database behave like chemistry?*

`figures/fig_database.py`, lines 80&ndash;81:

```python
    ranked_bars(panel[5], unrouted, accent=EMPHASIS,
                xlabel=f"solvent, {int(chemistry['unrouted solvents'].sum()):,} unrouted")
```

### `fig_chemistry` &rarr; `artifacts/figures/fig_chemistry.pdf`

| panel | computed by | which reads |
| --- | --- | --- |
| **a-g** The conditions the corpus reports, one field per panel | `checks/**/chemistry.py` | corpus = 'corpus_markdown'; extraction = DATABASE |
| **h** Yield against conversion, an identity the data has to obey | `checks/**/chemistry.py` | corpus = 'corpus_markdown'; extraction = DATABASE |
| **i** Our extraction against hand curation, on the papers both cover | `checks/**/gao_overlap.py` | crosswalk = PAPERS; gao = GAO |

#### Panel a-g — The conditions the corpus reports, one field per panel

The check behind it asks: *Does the extracted database behave like chemistry?*

`figures/fig_chemistry.py`, lines 42&ndash;47:

```python
    for index, column, label, clip, source in spec:
        reported = int(source[column].notna().sum())
        histogram(panel[index], source, column, clip=clip,
                  xlabel=f"{label}, n={reported:,}",
                  ylabel="records" if index % 3 == 0 else "", **stack)
        headroom(panel[index], stack_tops(panel[index]))
```

#### Panel h — Yield against conversion, an identity the data has to obey

The check behind it asks: *Does the extracted database behave like chemistry?*

`figures/fig_chemistry.py`, lines 57&ndash;57, 58&ndash;58, 59&ndash;59, 60&ndash;60, 61&ndash;61, 62&ndash;64, 65&ndash;65, 74&ndash;74:

```python
    cloud(panel[7], pair.conversion_percent, pair.yield_percent, colour=RAMP[2], size=1.4)

    panel[7].set_xlim(0, 100)

    panel[7].set_ylim(0, 100)

    panel[7].set_xlabel("conversion (%)")

    panel[7].set_ylabel("yield (%)")

    # A casing under the reference: unavoidably it runs through the densest part of the cloud,
    # and 0.35 pt of white a side is what keeps it a line rather than one more mark.
    panel[7].plot([0, 100], [0, 100], color="white", lw=1.5, zorder=3)

    panel[7].plot([0, 100], [0, 100], color=INK, lw=0.8, zorder=4)

    panel[7].plot([0, 100 - slack], [slack, 100], color=DIM, lw=0.5, ls=(0, (3, 2)), zorder=4)
```

#### Panel i — Our extraction against hand curation, on the papers both cover

The check behind it asks: *Our automatic extraction against Gao et al.'s hand-curated ionic-liquid glycolysis set.*

`figures/fig_chemistry.py`, lines 87&ndash;90, 91&ndash;93, 94&ndash;94, 95&ndash;95, 96&ndash;96, 97&ndash;97:

```python
    drawn = overlap(panel[8],
                    (ours.temperature_c, ours.yield_percent),
                    (curated.temperature_c, curated.yield_percent),
                    view=((140, 205), (0, 100)))

    # the frame comes from the shapes, not from the view: a patch does not autoscale its axes,
    # and clipping to the view cut both regions off where they ran past it
    panel[8].set_xlim(*drawn["bounds"][0])

    panel[8].set_ylim(*drawn["bounds"][1])

    panel[8].set_xlabel("temperature (°C)")

    panel[8].set_ylabel("yield (%)")

    panel[8].set_title("i", loc="left", fontsize=9, fontweight="bold", pad=5)
```

### `fig_graders` &rarr; `artifacts/figures/fig_graders.pdf`

| panel | computed by | which reads |
| --- | --- | --- |
| **a** The heuristic grader on each extractor, with the spread over repeats | `checks/**/extractions.py` | answer_key = CURATED; extractions = RUNS_DIR / 'extract_*' |
| **b** Judge against heuristic, on the pair the database ships | `checks/**/matrix.py` | answer_key = CURATED; extractions = RUNS_DIR / 'extract_*'; judges = RUNS_DIR / 'judge_*_on_*' |
| **c** On the disagreements, who the chemists backed | `checks/**/adjudicated.py` | decisions = path; extraction = worklist.EXTRACTION; judge = worklist.VERDICTS |

#### Panel a — The heuristic grader on each extractor, with the spread over repeats

The check behind it asks: *Every benchmark extraction scored against the curated answer key.*

`figures/fig_graders.py`, lines 53&ndash;55, 56&ndash;56:

```python
    grouped_bars(panel[0], models, errors=spread, names=SHORT,
                 palette=dict(zip(models.columns, CYCLE)),
                 ylabel="score vs answer key")

    panel[0].set_ylim(0, 1.05)
```

#### Panel b — Judge against heuristic, on the pair the database ships

The check behind it asks: *The judge x extraction agreement matrix -- the paper's headline table.*

`figures/fig_graders.py`, lines 61&ndash;62, 63&ndash;63:

```python
    pie(panel[1], __import__("pandas").Series(split),
        colours=[GRADER["metric"], GRADER["judge"], RAMP[3]], gap=0.34, span=1.7)

    panel[1].set_xlabel(f"judge vs heuristic, of {int(cell['records'])} records")
```

#### Panel c — On the disagreements, who the chemists backed

The check behind it asks: *What the chemists' decisions say about the two graders.*

`figures/fig_graders.py`, lines 69&ndash;70, 71&ndash;71, 72&ndash;72, 73&ndash;73, 74&ndash;75, 76&ndash;76, 77&ndash;77, 78&ndash;80:

```python
    panel[2].bar(range(2), [value for _, value, _ in backed], width=BAR,
                 color=[colour for *_, colour in backed])

    panel[2].set_xticks(range(2), [name for name, *_ in backed])

    panel[2].tick_params(axis="x", length=0)

    panel[2].set_xlabel("the chemists sided with")

    panel[2].set_ylabel(f"number of disagreements "
                        f"(of {mcnemar['informative pairs']})")

    headroom(panel[2], tallest * 1.3)

    panel[2].set_xlim(-0.6, 1.6)

    for position, (_, value, _) in enumerate(backed):
        panel[2].annotate(f"{value}", (position, value), textcoords="offset points",
                          xytext=(0, 3), ha="center", fontsize=6.5, color=DIM)
```

## Every macro the manuscript can quote

221 defined, 75 quoted by the manuscript today. A macro nothing quotes is still computed and still checked; it is available rather than dead.

| macro | value | computed by | quoted | what it is |
| --- | --- | --- | --- | --- |
| `\AdjAcceptedAnswered` | `47` | `adjudicated` | no | Of those, how many came back answered. The divisor for the weight. |
| `\AdjAcceptedDrawn` | `50` | `worklist` | no | Records drawn from the both-accepted pool for the page. |
| `\AdjAcceptedMovedIn` | `2` | `adjudicated` | no | Censused on the page but in the accepted cell today, because the solvent-alias fix landed afterwards. They keep weight 1: they were never sampled. |
| `\AdjAcceptedPool` | `189` | `adjudicated` | no | The pool those were drawn from. |
| `\AdjAcceptedReviewed` | `49` | `adjudicated` | yes | Records in the both-accepted cell under today's verdicts. |
| `\AdjAcceptedSampled` | `47` | `adjudicated` | no | In that cell both by design and today. |
| `\AdjAcceptedWeight` | `4.02` | `adjudicated` | no | What each answered record stands for: pool over answered. |
| `\AdjBothFlagged` | `12` | `adjudicated` | yes | Records the page censused because both graders flagged them. |
| `\AdjCarriedOver` | `20` | `adjudicated` | no | Records carried in pre-filled from the rescue round and left alone. |
| `\AdjDecidedFresh` | `92` | `adjudicated` | no | Records a chemist actively decided. |
| `\AdjDesignAccepted` | `47` | `adjudicated` | no | Records the page sampled from the both-accepted cell. |
| `\AdjDesignDisagree` | `53` | `adjudicated` | no | Records the page censused as grader disagreements. |
| `\AdjFavourJudge` | `43` | `adjudicated` | yes | Discordant pairs won by the judge. |
| `\AdjFavourMetric` | `8` | `adjudicated` | yes | Discordant pairs won by the metric. |
| `\AdjJudgeFlagged` | `15` | `adjudicated` | yes | Records the judge called incorrect. |
| `\AdjJudgeFone` | `0.33` | `adjudicated` | yes | Judge F1. As wide as recall. |
| `\AdjJudgeKappa` | `0.30` | `adjudicated` | no | Judge agreement with the chemists beyond chance. Low, because most records are correct. |
| `\AdjJudgePrecision` | `0.27` | `adjudicated` | yes | Judge precision. Censused, so it carries no sampling error. |
| `\AdjJudgeRecall` | `0.44` | `adjudicated` | yes | Judge recall. Weighted, and wide, because its denominator reaches into the sampled pool. |
| `\AdjJudgeTruePositives` | `4` | `adjudicated` | yes | Of the judge's flags, how many a chemist agreed were wrong. |
| `\AdjJudgeWinRate` | `84` | `adjudicated` | no | Judge wins as a share of discordant pairs. |
| `\AdjMcNemarP` | `7\times10^{-7}` | `adjudicated` | yes | McNemar exact p on those pairs. Every pair is censused, so this carries no sampling error. |
| `\AdjMetricFlagged` | `60` | `adjudicated` | yes | Records the metric grader called incorrect. |
| `\AdjMetricFone` | `0.26` | `adjudicated` | yes | Metric F1. |
| `\AdjMetricKappa` | `0.21` | `adjudicated` | no | Metric agreement beyond chance. |
| `\AdjMetricPrecision` | `0.15` | `adjudicated` | yes | Metric precision. |
| `\AdjMetricRecall` | `1.00` | `adjudicated` | yes | Metric recall. |
| `\AdjMetricTruePositives` | `9` | `adjudicated` | yes | Of the metric's flags, how many a chemist agreed were wrong. |
| `\AdjMissRateBound` | `6` | `adjudicated` | no | Rule of three. No wrong record among the answered ones bounds the hidden rate at roughly this, which is why metric recall reads 1.00 with a wide lower bound. |
| `\AdjNowDisagree` | `51` | `adjudicated` | no | Records the graders still disagree on under today's verdicts. |
| `\AdjPairs` | `51` | `adjudicated` | yes | Discordant pairs: records where exactly one grader matched the chemists. The only records that say which grader is better. |
| `\AdjPowerAtEighty` | `1.00` | `adjudicated` | no | Power at 80%. |
| `\AdjPowerAtSeventy` | `0.84` | `adjudicated` | no | Power at 70%. |
| `\AdjPowerAtSeventyFive` | `0.96` | `adjudicated` | no | Power at 75%. |
| `\AdjPowerAtSixty` | `0.30` | `adjudicated` | no | Power to detect a judge right 60% of the time, at the pairs returned. |
| `\AdjPowerAtSixtyFive` | `0.58` | `adjudicated` | no | Power at 65%. |
| `\AdjPriorPairs` | `4` | `adjudicated` | no | Discordant pairs the earlier random round produced. Too few for any outcome to reach significance -- the reason this round censused disagreements instead. |
| `\AdjRecords` | `112` | `adjudicated` | yes | Records the chemists ruled on. |
| `\AnnotatorsIndependent` | `0` | `integrity` | no | Of those, how many both actively decided rather than leaving a pre-fill. Zero here, so this round supports no inter-annotator agreement figure and none should be quoted. |
| `\AnnotatorsShared` | `27` | `integrity` | no | Records both chemists touched. |
| `\ArtifactHash` | `74a63eabd1dc` | `paper_numbers` | no | Hash over the config content-hashes of every run behind these numbers. Change a run and this changes, so the manuscript declares which artifact set it describes. |
| `\BenchLunaFone` | `0.805` | `extractions` | no | Luna F1 on the curated papers, averaged over repeats. |
| `\BenchLunaRuns` | `3` | `extractions` | no | Repeats behind it. |
| `\BenchLunaSd` | `0.018` | `extractions` | no | Its spread across repeats -- wider than some gaps between models. |
| `\BenchLunaShots` | `1` | `extractions` | no | Worked examples used. |
| `\BenchOssFone` | `0.754` | `extractions` | no | OSS F1. |
| `\BenchOssRuns` | `3` | `extractions` | no | OSS repeats. |
| `\BenchOssSd` | `0.034` | `extractions` | no | OSS spread. |
| `\BenchOssShots` | `1` | `extractions` | no | OSS worked examples. |
| `\BenchRepeats` | `3` | `extractions` | no | Repeats per arm. |
| `\BenchShots` | `1` | `extractions` | no | Worked examples the benchmark was scored at. |
| `\BenchTerraFone` | `0.781` | `extractions` | no | Terra F1. |
| `\BenchTerraRuns` | `3` | `extractions` | no | Terra repeats. |
| `\BenchTerraSd` | `0.004` | `extractions` | no | Terra spread. |
| `\BenchTerraShots` | `1` | `extractions` | no | Terra worked examples. |
| `\BenchmarkInCorpus` | `12` | `integrity` | no | Benchmark papers that also sit inside the mass corpus. |
| `\CitationsCopied` | `126` | `provenance` | no | Unresolved because copied verbatim from a worked example in the prompt. |
| `\CitationsNoMatch` | `370` | `provenance` | no | Unresolved and matching no chunk anywhere. |
| `\CitationsResolved` | `13,602` | `provenance` | yes | Citations pointing at a chunk that exists in the paper the record came from. |
| `\CitationsTotal` | `14,098` | `provenance` | yes | Chunk citations across the database. |
| `\CitationsTraceable` | `96.5` | `provenance` | yes | Resolved as a share of all citations. |
| `\CitationsUnresolved` | `496` | `provenance` | no | Citations resolving to nothing. Marked in the release, not dropped. |
| `\CitationsUntraceable` | `3.5` | `provenance` | yes | Unresolved as a share -- the complement of CitationsTraceable. |
| `\ConstraintCaught` | `72` | `integrity` | no | Flagged as a share -- the judge's hit rate on records that are provably wrong. |
| `\ConstraintFlagged` | `34` | `integrity` | no | Of those, how many the judge also flagged. |
| `\ConstraintOverConversionCaught` | `54` | `integrity` | no | Hit rate on yields above conversion. |
| `\ConstraintOverHundredCaught` | `100` | `integrity` | no | Hit rate on yields above 100%. |
| `\ConstraintRecords` | `47` | `integrity` | no | Records wrong on arithmetic alone. |
| `\ConversionCoverage` | `48` | `chemistry` | no | Share of records reporting conversion. A ceiling on what the data can support. |
| `\CorpusCandidates` | `2,777` | `corpus` | yes | Scopus hits before any filter. The top of the funnel. |
| `\CorpusConverted` | `1,027` | `corpus` | yes | Papers actually obtained and turned into chunked text. Below CorpusFiltered because of access, not relevance. |
| `\CorpusElsevierXml` | `447` | `corpus` | yes | Obtained as Elsevier full-text XML. |
| `\CorpusEuropePmc` | `89` | `corpus` | yes | Obtained as Europe PMC JATS deposits. |
| `\CorpusFiltered` | `1,575` | `corpus` | yes | Papers passing the title and abstract filter -- how many relevant papers exist. The denominator for every coverage claim. |
| `\CorpusNotObtained` | `548` | `corpus` | yes | Filtered minus converted: papers we know exist and cannot read. Concentrated in ACS and RSC. |
| `\CorpusOtherSource` | `3` | `corpus` | no | Obtained by no other named route. |
| `\CorpusPdf` | `488` | `corpus` | yes | Obtained as PDF: Wiley and Springer under entitlement, the rest open access. |
| `\CorpusUnextracted` | `1` | `corpus` | no | Converted minus extracted. One paper exhausted its rate-limit retries, so the corpus and the database differ by one and both numbers are reported. |
| `\CorrectedRecords` | `5,468` | `verdicts` | no | Rows in the corrected database: judged minus dropped. |
| `\CostLuna` | `0.18` | `cost`, `extractions` | no | USD to extract the curated papers with Luna. |
| `\CostTerra` | `1.23` | `cost`, `extractions` | no | The same with Terra, which scores lower and costs more. |
| `\CuratedExperiments` | `295` | `_setup` | yes | Experiments in the hand-curated answer key, including the entries the supervisors confirmed in the rescue round. Every metric-grader score is computed against this. |
| `\CuratedOverlapGlycolysis` | `116` | `release_numbers` | no | of the covered rows, how many are glycolysis |
| `\CuratedOverlapPapers` | `11` | `release_numbers` | no | curated dois that also appear in the release |
| `\CuratedOverlapRecords` | `116` | `release_numbers` | no | release rows from a curated paper |
| `\CuratedOverlapShare` | `2.9` | `release_numbers` | no | those rows as a share of the release |
| `\CuratedPapers` | `24` | `_setup` | yes | Papers the curated table covers. |
| `\DatabaseCost` | `8.63` | `json` | yes | USD spent on the mass extraction. |
| `\DatabaseEmpty` | `362` | `corpus` | yes | Papers that produced none. Mostly reviews and enzymatic work, i.e. papers that should yield none -- but this is also the audit's blind spot, since a missed experiment is invisible. |
| `\DatabaseLargestPaper` | `84` | `corpus` | yes | Records from the single largest paper. |
| `\DatabaseMedianRecords` | `5` | `corpus` | yes | Median records per yielding paper. |
| `\DatabasePapers` | `1,026` | `corpus` | yes | Papers the extractor processed. |
| `\DatabaseRecords` | `5,563` | `corpus` | yes | Records extracted across the whole corpus. |
| `\DatabaseShots` | `1` | `json` | no | Worked examples in the prompt for the shipped run. |
| `\DatabaseYielding` | `664` | `corpus` | yes | Papers that produced at least one record. |
| `\DistinctCatalysts` | `1,110` | `chemistry` | yes | Distinct catalyst strings. Large and long-tailed, which is what defeats a grader comparing catalysts by spelling. |
| `\ExemplarPool` | `7` | `integrity` | no | Papers the prompt's worked examples are drawn from. |
| `\ExemplarsInBenchmark` | `7` | `integrity` | no | Of those, how many are also benchmark papers. Leakage if not zero. |
| `\FieldSecondCount` | `442` | `verdicts` | yes | Corrections to the second most-corrected field (solvent mass). |
| `\FieldTableRows` | `(table rows)` | `verdicts` | no | — |
| `\FieldTopCount` | `628` | `verdicts` | yes | Corrections to the most-corrected field (catalyst mass). |
| `\FunnelDroppedOffTopic` | `271` | `corpus` | yes | Dropped: off-topic subject or a review. |
| `\FunnelDroppedPolymer` | `550` | `corpus` | yes | Dropped: no polymer named. |
| `\FunnelDroppedRoute` | `381` | `corpus` | yes | Dropped: no depolymerisation route named. |
| `\FunnelPassRate` | `56.7` | `corpus` | yes | Share of candidates surviving the filter. |
| `\GaoChart` | `173` | `gao_overlap` | no | Gao records that are values read off a plotted curve. Our prompt reads tables and ignores figures, so this is a scope decision rather than a miss. |
| `\GaoChartShare` | `48` | `gao_overlap` | no | Share of Gao's set read off a plotted curve rather than a table. Our prompt instructs the model to read tables and ignore figures, so this gap is a scope decision. |
| `\GaoIdenticalConditions` | `118` | `gao_overlap` | no | Matched pairs whose temperature is byte-identical on both sides. Reaction time is the same count. |
| `\GaoIdenticalPairs` | `118` | `gao_overlap` | no | Matched pairs reporting temperature at all, the denominator for that. |
| `\GaoMissed` | `12` | `gao_overlap` | no | Gao records tabulated in text the model read and did not pick up. The real extraction shortfall. |
| `\GaoMissedShare` | `3` | `gao_overlap` | no | Those as a share of Gao's set. |
| `\GaoOursOnly` | `91` | `gao_overlap` | no | Tabulated experiments we hold on those papers that the hand curation does not. |
| `\GaoPapers` | `19` | `gao_overlap` | yes | Papers those came from, identified from Gao's short labels by hand. |
| `\GaoParityAgree` | `239` | `gao_overlap` | no | Of those, how many agree within GaoParitySlack percentage points. |
| `\GaoParityPairs` | `239` | `gao_overlap` | no | Matched outcome values -- yield, conversion, selectivity -- with a number on both sides. All three are percentages, so one parity axis serves them. |
| `\GaoParitySlack` | `2` | `gao_overlap` | no | Percentage points of slack in that comparison. Absolute, not relative: these are all percentages, and a relative tolerance calls 1% against 3% a disagreement. |
| `\GaoReclassified` | `14` | `gao_overlap` | no | Automatic reasons a chemist reading the paper overruled. |
| `\GaoRecords` | `364` | `gao_overlap` | no | Experiments Gao et al. curated by hand from the ionic-liquid glycolysis literature. |
| `\GaoRule` | `18` | `gao_overlap` | no | Gao records from response-surface design tables our scope excludes. |
| `\GaoRuleOverruled` | `9` | `gao_overlap` | no | Records the automatic rule called our extraction's miss that a chemist reading the paper reclassified as chart-read or out of scope. The shortfall is human-reduced. |
| `\GaoShared` | `131` | `gao_overlap` | yes | Experiments both datasets describe, so agreement can be measured on them. |
| `\GaoSi` | `30` | `gao_overlap` | no | Gao records sitting in Supporting Information we do not hold. |
| `\GaoWorstShare` | `87` | `gao_overlap` | no | Agreement on the weakest field. Every other field is above it. |
| `\GrowthAllAdded` | `46` | `growth` | no | How many that added. |
| `\GrowthAllExperiments` | `341` | `growth` | no | Key size after promoting every unmatched record. |
| `\GrowthAllFone` | `0.883` | `growth` | no | F1 against it. |
| `\GrowthAllPrecision` | `0.890` | `growth` | no | Precision against that key. |
| `\GrowthCuratedFone` | `0.802` | `growth` | no | F1 against the same. |
| `\GrowthCuratedPrecision` | `0.753` | `growth` | no | Precision against the curated key as it stands. |
| `\GrowthVouchedAdded` | `43` | `growth` | no | How many that added. |
| `\GrowthVouchedExperiments` | `338` | `growth` | no | Key size after promoting judge-accepted records into it. |
| `\GrowthVouchedFone` | `0.878` | `growth` | no | F1 against it. Higher without a single extracted record changing, which is why a key grown from the extraction's own output overstates quality. |
| `\GrowthVouchedPrecision` | `0.881` | `growth` | no | Precision against the grown key. |
| `\IdentityImpossible` | `28` | `chemistry` | yes | Of those, records with yield above conversion. An identity, not a correlation. |
| `\IdentityPairs` | `1,423` | `chemistry` | yes | Records reporting both yield and conversion, so the identity can be tested. |
| `\JudgeAccepted` | `3,812` | `verdicts` | yes | Verdict 'correct': accepted with nothing changed. |
| `\JudgeContextCap` | `131{,}072` | `_setup` | yes | The judge model's context window, in tokens. |
| `\JudgeCorrected` | `1,656` | `verdicts` | yes | Rejected with a proposed replacement value. The record survives into the corrected database with those fields rewritten. |
| `\JudgeCorrectedShare` | `29.8` | `verdicts` | yes | Corrected as a share of judged. |
| `\JudgeDropped` | `95` | `verdicts` | yes | Rejected outright -- the judge set drop_record, so cli/release.py deletes the row rather than repairing it. Partitioned on that flag, because that is what the released files act on. |
| `\JudgeDroppedShare` | `1.7` | `verdicts` | yes | Dropped as a share of judged. |
| `\JudgeFieldFixes` | `2,812` | `verdicts` | yes | Field-level changes proposed, larger than JudgeCorrected because one record can carry several. |
| `\JudgeOverContext` | `0` | `corpus` | no | Papers that exceeded that window. Zero, so no verdict was truncated. |
| `\JudgePassRate` | `68.5` | `verdicts` | yes | Accepted as a share of judged. The headline quality figure. |
| `\JudgeRecords` | `5,563` | `verdicts` | yes | Records the judge read. Equals DatabaseRecords; quoted separately where the sentence is about judging. |
| `\LabelledRecords` | `48` | `_setup` | no | Records in that earlier round. |
| `\LargestJudgedTokens` | `88,383` | `corpus` | no | Tokens in the longest paper judged, against the cap above. |
| `\MatrixEvaluableHigh` | `0.940` | `matrix` | no | Highest. |
| `\MatrixEvaluableLow` | `0.674` | `matrix` | no | Lowest evaluable-record agreement of the nine cells. |
| `\MatrixEvaluableRows` | `(table rows)` | `matrix` | yes | — |
| `\MatrixGapHigh` | `12` | `matrix` | no | Largest. The gap exists because the metric marks surplus records wrong without looking at them, not because of chemistry. |
| `\MatrixGapLow` | `0` | `matrix` | no | Smallest gap between all-record and evaluable-record agreement. |
| `\MatrixShippedAll` | `0.811` | `matrix` | no | Judge-metric agreement for the shipped pair, over every record. |
| `\MatrixShippedEvaluable` | `0.935` | `matrix` | yes | The same, over records the metric could evaluate. |
| `\MatrixTableRows` | `(table rows)` | `matrix` | no | — |
| `\ReleaseAccepted` | `2,667` | `release_numbers` | no | audit == accepted |
| `\ReleaseBaselines` | `798` | `release_numbers` | no | rows with phase == uncatalysed, kept as controls |
| `\ReleaseCatalystNames` | `511` | `release_numbers` | no | distinct catalyst strings among catalysed rows |
| `\ReleaseConfirmed` | `796` | `release_numbers` | no | structure_source == confirmed, i.e. OPSIN derived it from the name |
| `\ReleaseConfirmedShare` | `20.1` | `release_numbers` | no | as a share of all rows |
| `\ReleaseDuplicates` | `443` | `release_numbers` | no | rows repeating an earlier experiment in the same paper |
| `\ReleaseExcludedSolid` | `1,109` | `release_numbers` | no | source records whose catalyst is classed heterogeneous |
| `\ReleaseExcludedUnknown` | `503` | `release_numbers` | no | source records whose catalyst resolves to no compound |
| `\ReleaseFeaturisable` | `3,404` | `release_numbers` | no | rows with a catalyst structure, or honestly none |
| `\ReleaseFlagged` | `1,284` | `release_numbers` | no | audit == flagged |
| `\ReleaseGlycolysis` | `1,537` | `release_numbers` | no | route == glycolysis |
| `\ReleaseHash` | `dfc127758cb8` | `release_numbers` | no | sha256 of the released CSV, first 12 hex digits |
| `\ReleaseHomogeneousRecords` | `3,153` | `release_numbers` | no | rows with phase == homogeneous |
| `\ReleaseHydrolysis` | `1,149` | `release_numbers` | no | route == hydrolysis |
| `\ReleaseLLMWritten` | `1,827` | `release_numbers` | no | structure_source == LLM written, nothing independent checked it |
| `\ReleaseLLMWrittenShare` | `46.2` | `release_numbers` | no | as a share of all rows |
| `\ReleaseMethanolysis` | `386` | `release_numbers` | no | route == methanolysis |
| `\ReleaseModelable` | `850` | `release_numbers` | no | rows with yield, temperature, time and both masses |
| `\ReleaseModelablePapers` | `171` | `release_numbers` | no | distinct doi among those rows |
| `\ReleaseMultiComponent` | `155` | `release_numbers` | no | catalyst cell names more than one substance |
| `\ReleaseNoRoute` | `879` | `release_numbers` | no | solvent matches none of the three rules, so no product is fixed |
| `\ReleasePapers` | `561` | `release_numbers` | yes | distinct doi |
| `\ReleasePassRate` | `67.5` | `release_numbers` | no | accepted over judged, not over all rows |
| `\ReleaseQualityFlagged` | `294` | `release_numbers` | no | rows failing a range or identity check |
| `\ReleaseRecords` | `3,951` | `release_numbers` | yes | row count |
| `\ReleaseSolvents` | `239` | `release_numbers` | no | distinct solvent strings |
| `\ReleaseStructures` | `180` | `release_numbers` | no | distinct catalyst_smiles; multi-component rows carry none |
| `\ReleaseVersion` | `1.0` | `release_numbers` | no | written by build_release.py |
| `\ReleaseWithYield` | `1,880` | `release_numbers` | no | yield_percent not null |
| `\ReleaseYieldShare` | `47.6` | `release_numbers` | no | yield_percent not null, as a share of all rows |
| `\RouteGlycolysis` | `2,649` | `chemistry` | no | Records whose solvent implies glycolysis. |
| `\RouteHydrolysis` | `1,415` | `chemistry` | no | Records whose solvent implies hydrolysis. |
| `\RouteMethanolysis` | `577` | `chemistry` | no | Records whose solvent implies methanolysis. |
| `\RouteNamedTotal` | `4,641` | `chemistry` | no | The three named routes summed. With RouteOther it accounts for the corpus. |
| `\RouteOther` | `922` | `chemistry` | no | Records whose solvent names no route, so no product is fixed. |
| `\RouteRatio` | `1.3` | `chemistry` | yes | Glycolysis records divided by hydrolysis plus methanolysis. The corpus is lopsided, which is why the grader study was built on glycolysis. |
| `\SelectivityCoverage` | `4` | `chemistry` | yes | Share of records reporting selectivity: the thinnest outcome field. |
| `\ShotsAnovaP` | `0.03` | `shots` | no | One-way ANOVA among the arms that have examples: nothing separates them. |
| `\ShotsBest` | `1` | `shots` | no | Arm with the highest mean. One, which is why the database ships at one. |
| `\ShotsBonferroni` | `0.005` | `shots` | no | Bonferroni-corrected alpha for that many comparisons. |
| `\ShotsComparisons` | `10` | `shots` | no | Pairwise comparisons available among the arms. |
| `\ShotsDelta` | `+0.025` | `shots` | yes | F1 gain from one worked example over none. |
| `\ShotsP` | `0.082` | `shots` | yes | p for that gain. |
| `\ShotsSmallestPairwise` | `0.02` | `shots` | no | Smallest pairwise p among them, against that alpha. |
| `\SourceCiting` | `0.780` | `source_tracking` | yes | F1 with citations required. |
| `\SourceCitingSd` | `0.021` | `source_tracking` | yes | Its spread. |
| `\SourceDelta` | `+0.064` | `source_tracking` | yes | F1 gain from requiring source citations. |
| `\SourceNot` | `0.716` | `source_tracking` | yes | F1 without. |
| `\SourceNotSd` | `0.043` | `source_tracking` | yes | Its spread. |
| `\SourceP` | `0.079` | `source_tracking` | yes | p for the difference. Short of significance, reported as such. |
| `\ThresholdAcceptBest` | `0.5` | `thresholds` | no | Where the accept threshold would have peaked. |
| `\ThresholdAcceptGap` | `0.030` | `thresholds` | no | F1 left on the table by the accept threshold. Reported and not taken: moving it now, to maximise a score on the papers the score is computed from, is the tuning we claim not to do. |
| `\ThresholdCatalystBest` | `0.4` | `thresholds` | no | Where it would have peaked. |
| `\ThresholdCatalystGap` | `0.020` | `thresholds` | no | The same for the catalyst gate. |
| `\ThresholdToleranceBest` | `0.5` | `thresholds` | no | Where it would have peaked. |
| `\ThresholdToleranceGap` | `0.017` | `thresholds` | no | The same for the numeric tolerance. |
| `\WithinPaperP` | `7\times10^{-7}` | `withinpaper` | yes | Wilcoxon signed-rank p for those within-paper correlations against zero. Was typed into the body by hand as 7e-7 while the check had been returning it all along. |
| `\WithinPaperPapers` | `59` | `withinpaper` | no | Papers with enough records to compute that correlation. |
| `\WithinPaperPositive` | `85` | `withinpaper` | yes | Share of those papers where the correlation runs the predicted way. |
| `\WithinPaperRho` | `0.34` | `withinpaper` | yes | Median within-paper Spearman rho for temperature against yield. Positive, i.e. the expected chemistry, once each paper is compared only with itself. |
| `\WithinPaperShare` | `9` | `corpus`, `withinpaper` | no | Those papers as a share of all yielding papers. |
| `\WithinPooledN` | `2,742` | `withinpaper` | no | Records behind the pooled figure. |
| `\WithinPooledRho` | `+0.018` | `withinpaper` | yes | The same correlation pooled across every paper: near zero. Pooling is the wrong operation on optimised experiments, and the contrast with WithinPaperRho is the point. |
| `\WithinRoutePairs` | `12` | `withinpaper` | no | Route-relationship pairs whose median within-paper rho runs the way chemistry predicts. Strictly: an exactly-zero median counts for neither side. |
| `\WithinRouteTotal` | `12` | `withinpaper` | no | Route-relationship pairs tested: four relationships in each of three routes. |
| `\YieldOverHundred` | `19` | `chemistry` | no | Records reporting a yield above 100%, which is impossible. |

## The checks, and what each one reads

- **`checks/curated/answer_key.py`** — *The curated answer key: how big it is, what it leaves blank, and whether it holds together.*  
  reads: answer_key = CURATED
- **`checks/curated/extractions.py`** — *Every benchmark extraction scored against the curated answer key.*  
  reads: answer_key = CURATED; extractions = RUNS_DIR / 'extract_*'
- **`checks/curated/field_types.py`** — *Why the two graders agree as much as they do.*  
  reads: answer_key = CURATED; extraction = EXTRACTION; judge = JUDGE
- **`checks/curated/gao_overlap.py`** — *Our automatic extraction against Gao et al.'s hand-curated ionic-liquid glycolysis set.*  
  reads: gao = GAO; crosswalk = PAPERS
- **`checks/curated/judged_runs.py`** — *What each judge said about each extraction, on the curated papers.*  
  reads: answer_key = CURATED; extractions = RUNS_DIR / 'extract_*'; judges = RUNS_DIR / 'judge_*_on_*'
- **`checks/curated/matrix.py`** — *The judge x extraction agreement matrix -- the paper's headline table.*  
  reads: answer_key = CURATED; extractions = RUNS_DIR / 'extract_*'; judges = RUNS_DIR / 'judge_*_on_*'
- **`checks/curated/metric.py`** — *Is the metric grader behaving, and where does it lose records?*  
  reads: answer_key = CURATED; extraction = EXTRACTION
- **`checks/curated/shots.py`** — *Does showing the model more worked examples help?*  
  reads: answer_key = CURATED; runs = RUNS_DIR / FOLDER
- **`checks/curated/source_tracking.py`** — *Does requiring the model to cite its source chunks change what it extracts?*  
  reads: answer_key = CURATED; on = RUNS_DIR / ARMS['citing sources']; off = RUNS_DIR / ARMS['not citing']
- **`checks/curated/thresholds.py`** — *Answers 'did you tune the thresholds until the number looked good?'.*  
  reads: answer_key = CURATED; extraction = EXTRACTION
- **`checks/database/chemistry.py`** — *Does the extracted database behave like chemistry?*  
  reads: corpus = 'corpus_markdown'; extraction = DATABASE
- **`checks/database/corpus.py`** — *The funnel from search to corpus, and what the extraction produced on it.*  
  reads: funnel = 'corpus_candidates.csv'; corpus = 'corpus_markdown'; extraction = DATABASE
- **`checks/database/formats.py`** — *Does it matter which format a paper arrived in?*  
  reads: corpus = 'corpus_markdown'; extraction = DATABASE; judge = DATABASE_JUDGE
- **`checks/database/provenance.py`** — *Do the records point at text that exists?*  
  reads: corpus = 'corpus_markdown'; extraction = DATABASE
- **`checks/database/verdicts.py`** — *The judge's verdicts on the database.*  
  reads: extraction = DATABASE; judge = DATABASE_JUDGE
- **`checks/database/withinpaper.py`** — *Does the chemistry appear when each paper is compared with itself?*  
  reads: corpus = 'corpus_markdown'; extraction = DATABASE
- **`checks/human/adjudicated.py`** — *What the chemists' decisions say about the two graders.*  
  reads: extraction = worklist.EXTRACTION; judge = worklist.VERDICTS; decisions = path
- **`checks/human/growth.py`** — *What happens to the extraction's score as records are added to the answer key.*  
  reads: labels = LABELS; labelled_run = LABELLED; answer_key = CURATED; judge = JUDGE
- **`checks/human/integrity.py`** — *Three things a referee will ask that the paper never answered.*  
  reads: extraction = DATABASE; judge = DATABASE_JUDGE; decisions = DECISIONS
- **`checks/human/worklist.py`** — *Which records the next adjudication round should use, and how many.*  
  reads: extraction = EXTRACTION; judge = VERDICTS
- **`checks/release/quality.py`** — *Twenty questions about the released dataset, and what is wrong with it.*  
  reads: —
- **`checks/release/release_numbers.py`** — *Every number the manuscript quotes about the released dataset, with its derivation.*  
  reads: —
- **`checks/cost.py`** — *The spend ledger across every run that still has a config.*  
  reads: runs = RUNS_DIR; configs = ROOT / 'configs'
- **`checks/palette.py`** — *Can a reader actually tell the figures' colours apart?*  
  reads: —
- **`checks/paper.py`** — *Does the manuscript body quote anything the checks did not compute?*  
  reads: macros = MACROS
